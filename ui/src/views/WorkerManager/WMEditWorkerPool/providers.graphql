query Providers($providersConnection: PageConnection, $filter: JSON) {
    providers(connection: $providersConnection, filter: $filter) {
        pageInfo {
          hasNextPage
        }
        edges {
            node {
                providerId
                providerType
            }
        }
    }
}
