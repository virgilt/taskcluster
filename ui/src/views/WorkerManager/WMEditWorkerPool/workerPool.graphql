query WorkerPool($workerPoolId: ID!) {
    workerPool(workerPoolId: $workerPoolId) {
        workerPoolId
        description
        owner
        emailOnError
        providerId
        config
        lastModified
        pendingTasks
    }
}
