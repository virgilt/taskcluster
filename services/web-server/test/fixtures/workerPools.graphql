query ListWorkerPools($connection: PageConnection, $filter: JSON){
  workerPools(connection: $connection, filter: $filter){
    edges{
      node{
        workerPoolId
        providerId
      }
    }
  }
}
