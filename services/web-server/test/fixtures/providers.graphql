query ListProviders($connection: PageConnection, $filter: JSON){
  providers(connection: $connection, filter: $filter){
    edges{
      node{
        providerId
        providerType
      }
    }
  }
}
